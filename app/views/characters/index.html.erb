<div class="row">
  <% @characters.each do |character| %>
    <div class="col-sm-6 col-md-3">
      <div class="thumbnail">
        <%= link_to (image_tag character.image.url(:medium), class: 'image'), character %>
      </div>
    </div>
  <% end %>
</div>
